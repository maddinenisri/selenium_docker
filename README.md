# Selenium With Node for Acceptance tests

```sh
docker buildx build --platform linux/arm64 -t java-selenium-chromium-test .
docker run --rm java-selenium-chromium-test

```
```sh
# Build the docker image
docker build -t java-selenium-chromium-test .

# Run the container
docker run --rm java-selenium-chromium-test

```
